# Anima Core
torch>=2.0.0
numpy
websockets

# Cloud / API
boto3
requests

# Sensors
opencv-python
pyaudio

# ML
transformers
accelerate
peft          # LoRA adapters for HFDecoder (v11 --d-engine hf, e.g. Mistral 7B)
tokenizers    # Native byte-fallback BPE dialogue model
huggingface_hub
pyarrow       # Pinned Parquet training-corpus preparation

# Utils
scipy
matplotlib
