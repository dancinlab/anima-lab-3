# CUE-ROBUST-1 — 손상 자료를 더한 기존 판독기 비교

상태: 완료 · `CR2_CONTEXT_CATEGORY_NOT_RECOVERED`
등록일: 2026-08-11
선행 결과: `CUE-MECHANISM-1=CM3_DUAL_CATEGORY_LOSS`

## 질문과 고정 가설

`CUE-MECHANISM-1`은 질문 상태의 좌표를 25% 가렸을 때 상황 범주 판독이
`85.16~86.04%`, 열쇠 범주 판독이 `82.32~82.91%`로 떨어지는 것이 공용 기억 실패의 첫
지점임을 확인했다. 이번 실험은 새 기억 복원 구조가 필요한지 묻기 전에, 기존 선형 판독기가
손상된 입력 예시를 학습하지 않았기 때문에 생긴 일반적인 입력 변화 문제인지 검사한다.

가설은 단순하다. `COMPONENT-2`의 온전한 학습 상태에 등록된 25% 좌표 가림 복사본만 더해 같은
결정형 판독기를 다시 맞추면, 보지 않은 가림표에서도 상황·열쇠 범주와 공용 기억 행동이 회복될
수 있다. 회복되면 최소 원인은 판독기의 손상 자료 부족이다. 회복되지 않으면 큰 복원 층을 붙이기
전에 남은 손실 위치를 다시 분해한다.

## 고정 구현과 비교

- `CUE-MECHANISM-1` 결과와 판정, `COMPONENT-2` 학습 결과와 상황·열쇠 체크포인트,
  `VALUE-2` 값 체크포인트, 행동 값 원형 체크포인트의 파일 지문을 실행 전에 확인한다.
- `COMPONENT-2`의 512회차, 두 엔진 시작값, 16개 사건, 자료 시작값, 감각 순서와 처리 횟수를
  그대로 다시 만든다. 학습·평가 회차 지문은 겹치지 않아야 한다.
- 원래 온전한 상황·열쇠 상태와 라벨을 그대로 수집한다. 온전한 자료만 `canonical_ridge`로 다시
  맞춘 결과는 기존 `COMPONENT-2` 체크포인트와 모든 값이 같아야 한다.
- 손상 대응 판독기는 온전한 각 상태와 그 상태의 25% 좌표를 가린 복사본을 같은 라벨로 함께
  사용한다. 상황과 열쇠에 같은 규칙을 적용한다.
- 학습 가림표는 `cue_robust1_train`이라는 별도 고정값으로 만든다. 평가는 선행 실험의
  `completion1` 가림표를 그대로 쓰며, 두 가림표의 완전 일치 개수는 0이어야 한다.
- 판독 방식 `canonical_ridge`, 입력 폭 96, 성분 주소 폭 32, 합성 주소 폭 64, 규제값, 편향,
  고정 직교 범주 중심은 바꾸지 않는다.
- 상황 처리 6회, 열쇠·값 처리 횟수, 사건 16개, 공용 `CompositeStateTransform → VectorMemory`
  호출을 유지한다. 저장 상태는 온전하며 질문 상태만 평가 가림표로 손상한다.
- `full_cue`, 상황만 25% 손상, 열쇠만 25% 손상, 두 성분 동시 25% 손상, 정확 주소, 내용
  맞바꾸기를 네 원형/엔진 시작값 조합에서 측정한다.
- 라벨을 고정 순환시킨 가짜 판독기는 평가 범주 정확도 25% 이하여야 한다.
- 주소 폭 확대, 새 복원 층, 학습 가능한 주의집중, 처리 횟수 변경, 50% 이상 가림 학습,
  결과를 본 뒤 자료·문턱 변경은 하지 않는다.

## 유효성 및 성공 기준

원본 자료·체크포인트·가림표·상태 모양·감각 호출·공용 변환 호출·주소 폭·값 변환이 등록값과
맞아야 한다. 학습 자료는 성분마다 온전한 상태와 25% 손상 상태가 정확히 1:1이고 라벨 수가
범주별로 같아야 한다. 같은 입력을 다시 맞추거나 입력 순서를 뒤집어 다시 맞춰도 체크포인트가
같아야 한다.

네 평가 조합 모두 다음을 통과해야 한다.

- 온전한 상황·열쇠 범주 정확도 90% 이상, 범주별 최저 맞힘률 75% 이상
- 25% 손상된 상황·열쇠 각각의 범주 정확도 90% 이상, 범주별 최저 맞힘률 75% 이상
- 온전한 입력의 기억 선택·최종 행동 90% 이상이며, 선행 온전한 범주 정확도보다 2%p 넘게
  떨어지지 않음
- 상황만·열쇠만·두 성분 동시 25% 손상의 기억 선택·최종 행동 각각 90% 이상
- 정확 주소 선택 99%·행동 90%·값별 최저 맞힘률 75% 이상
- 내용 맞바꾸기 행동 5% 이하, 공용 기억 반환과 직접 코사인 계산 일치
- 손상 자료를 빼고 다시 맞춘 판독기는 기존 체크포인트 및 선행 손실과 같음

판정 순서는 다음으로 고정한다.

- `CR1_ROBUST_CUE_PATH_VALID_NOT_UNIQUE`: 온전한 성능을 유지하며 두 범주와 모든 25% 손상
  기억 조건이 통과한다.
- `CR6_FULL_CUE_REGRESSION`: 손상 대응 뒤 온전한 범주 또는 공용 기억 기준이 무너진다.
- `CR4_DUAL_CATEGORY_NOT_RECOVERED`: 손상된 상황·열쇠 범주가 모두 회복되지 않는다.
- `CR2_CONTEXT_CATEGORY_NOT_RECOVERED`: 손상된 상황 범주만 회복되지 않는다.
- `CR3_KEY_CATEGORY_NOT_RECOVERED`: 손상된 열쇠 범주만 회복되지 않는다.
- `CR5_RETRIEVAL_NOT_RECOVERED`: 두 범주는 회복됐지만 한 개 이상의 손상 기억 조건이 실패한다.
- `CR0_INVALID`: 원본·자료·가림표·재맞춤 일치·결정성·가짜·양성·공용 호출 검사가 어긋난다.

성공하면 손상 대응 판독기를 기본값이 꺼진 선택 기능 후보로 보존하되, 이 실험 안에서 공용
기본값으로 바꾸지는 않는다. 실패하면 결과를 보고 큰 복원 층을 붙이지 않고 첫 남은 손실만 다음
실험으로 분리한다.

어떤 성공 판정도 생물학적 패턴 완성이나 의식을 증명하지 않는다. 이는 일반적인 손상 자료 학습이
기억 입력을 튼튼하게 만드는지 확인하는 표준 비교다.

결과를 보기 전 이 문서와 README를 커밋하고, 그 커밋을 실행 사양의
`preregistration_commit`으로 고정한다.

## 실행과 결과

사전등록 커밋은 `eee24d2f2`, 실행기와 닫힌 판정기 커밋은 `8af9b5771`이다. 원래
`COMPONENT-2` 학습 상태 16,384개에 성분별 25% 가림 복사본 16,384개를 더해, 상황과 열쇠
판독기를 각각 32,768개 상태로 다시 맞췄다. 각 범주의 상태는 정확히 4,096개였다.

| 원형/엔진 시작값 | 상황 손상 범주 / 행동 | 열쇠 손상 범주 / 행동 | 양쪽 손상 행동 | 온전한 행동 |
|---|---:|---:|---:|---:|
| 1337 / 1337 | 89.16% / 85.84% | 90.33% / 87.11% | 80.86% | 92.68% |
| 1337 / 7331 | 89.94% / 86.43% | 90.53% / 87.21% | 81.45% | 92.77% |
| 7331 / 1337 | 89.16% / 85.84% | 90.33% / 87.11% | 80.86% | 92.68% |
| 7331 / 7331 | 89.94% / 86.43% | 90.53% / 87.21% | 81.45% | 92.77% |

상황의 손상 범주 판독은 선행 결과보다 `3.91~4.00%p`, 열쇠는 `7.62~8.01%p` 올랐다. 상황만
손상한 행동은 `3.13~3.22%p`, 열쇠만은 `4.20~4.69%p`, 양쪽 손상은 `7.42~7.52%p`
개선됐다. 그러나 등록 기준은 네 조합 모두 범주와 행동 90% 이상이므로, 가까워진 수치만으로
통과 처리하지 않았다.

온전한 상황 범주 판독은 `96.19~96.48%`, 열쇠는 `98.54~98.83%`, 기억 선택은
`93.46~93.85%`였다. 온전한 행동은 선행보다 `0.49~0.59%p` 낮았지만 등록한 최대 허용 하락
2%p 안이며 모든 온전한 기준을 통과했다. 정확 주소는 선택 100%·행동 `98.93~99.22%`, 내용
맞바꾸기는 `1.37~1.46%`였다. 순환시킨 가짜 라벨의 손상 범주 정확도는 상황
`1.66~1.95%`, 열쇠 `0.68~0.78%`라 가짜 통제도 통과했다.

손상 자료를 빼고 같은 원자료만 다시 맞춘 상황·열쇠 판독기는 기존 `COMPONENT-2` 체크포인트와
모든 값이 같았다. 손상 대응 판독기도 같은 입력 재맞춤과 입력 역순 재맞춤에서 같은 값이 나왔다.
학습 가림표 16,384개와 평가 가림표 1,024개의 완전 일치는 두 성분 모두 0개였다. 주소 폭,
감각 처리, 저장 16회·검색 1회와 공용 반환은 등록값을 유지했다.

첫 실행은 계산을 모두 마쳤지만 디스크가 가득 차 결과 파일을 쓰지 못해 결과로 사용하지 않았다.
오래된 임시 가상환경과 다시 만들 수 있는 캐시만 정리한 뒤 같은 코드·자료·기준으로 처음부터
재실행했다. 정상 저장된 실행과 별도 경로의 독립 전체 재실행은 결과 JSON
`9d5a550afef62c73d487517fdfe42830327cdbbaa8f1aaa3a0ba8a097ded2d90`, 판정 JSON
`9a67d8d52ecb01973e498c18cb1a78e59955565d5d1845706dd60ec7dcb2b61e`로 바이트 단위까지
같았다. 체크포인트 SHA-256은
`048acc5b6241d0ea4720c56afe2788ae123eb0e830fb653936a368e2eff7f8c5`다.

## 판정과 의미

판정은 `CR2_CONTEXT_CATEGORY_NOT_RECOVERED`다. 손상 자료 추가는 열쇠 범주 판독을 90% 위로
회복시켰지만 상황 범주는 두 엔진 시작값에서 각각 89.16%, 89.94%로 기준을 통과하지 못했다.
사전등록한 첫 손실 우선순위에 따라, 뒤의 기억 검색 실패보다 상황 범주 미회복을 먼저 판정했다.

열쇠 범주가 통과했는데도 열쇠 단독 손상 행동이 87%대에 머문 사실은 연속 열쇠 주소의 거리
여유도 별도 문제임을 보여준다. 다만 현재 첫 실패는 상황 범주다. `COMPONENT-2` 학습 자료는
사건을 저장할 때의 상황 상태만 포함하지만, 평가는 방해 입력 뒤 질문할 때의 상황 상태를 읽는다.
다음 `CUE-CONTEXT-1`은 이 시점 차이를 먼저 분리한다. 같은 총 자료에서 `저장 상태만 / 질문과
같은 시점 상태 추가`와 `손상 없음 / 25% 손상 추가`를 교차 비교해, 손상 예시 부족과 상태 시점
차이 중 어느 쪽이 남은 상황 손실을 만드는지 확인한다. 그 뒤에만 열쇠 주소 거리 문제를 분리한다.

따라서 손상 대응 판독기를 공용 기본값으로 바꾸지 않는다. 새 복원 층이나 주소 폭 확대도 아직
추가하지 않는다. 이 결과는 일반적인 입력 손상 대응의 부분 개선이며 생물학적 패턴 완성이나
의식의 증거가 아니다.

결과·판정·손상 대응 판독기 체크포인트는 비공개 Hugging Face 조직 보관소
`dancinlab/anima-lab-research-archive`의 `cue-robust1-damage-augmented-readout` revision에
보존했다. Hugging Face 커밋은 `b3fd13ee75a1729ebcad4f7ce73c658324cbd64b`, manifest
SHA-256은 `89c0c63c43f34406a29c8e9ae5dbfe2d7717f9abe991c1982d21aea6c3611db0`다. 새 임시
디렉토리로 다시 내려받은 세 산출물과 manifest 자체 지문을 전수 확인했다.
